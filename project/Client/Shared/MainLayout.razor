@inject NavigationManager Navigation

@inherits LayoutComponentBase

<div class="container-fluid vh-100 d-flex flex-column">
    <div class="row flex-grow-1">
        
        @* <!-- Sidebar -->
        <nav class="col-12 col-md-3 col-lg-2 bg-dark text-white p-3 d-flex flex-column">
            <h2 class="fs-5 text-center mb-4">Menu</h2>
            <NavMenu />
        </nav> *@

        @if (Navigation.Uri.Contains("/admin/"))
        {
            <nav class="col-12 col-md-3 col-lg-2 bg-dark text-white p-3 d-flex flex-column">
                <h2 class="fs-5 text-center mb-4">Menu</h2>
                <NavMenu />
            </nav>
        }

        <!-- Main Content -->
        <main class="col p-0">
            @if(Navigation.Uri.Contains("/admin/")){
            }else{
                <header>
                    <div>
                        <TopBar />
                    </div>
                    <div class="bg-light border-bottom py-2 px-4 d-flex justify-content-between align-items-center">
                        <TopMenu />
                    </div>
                </header>
            }

            <!-- Body Content -->
            <section class="content p-4">
                @Body
            </section>
        </main>
    </div>
</div>